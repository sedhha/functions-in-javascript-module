// 1. const <ArrowFunctionName> = (<ArgumentList>) => {<Body>;}
// 2. const <ArrowFunctionName> = (<ArgumentList>) => <SingleLineExpression>;

const multiplyNumberByAnotherNumber = (number, anotherNumber) => {
  const result = number * anotherNumber;
  return result;
};
